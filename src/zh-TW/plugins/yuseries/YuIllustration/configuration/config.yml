Database:
  # MYSQL, SQLITE
  Method: MYSQL
  SQL:
    host: localhost
    port: 3306
    user: root
    password: mfl235515223
    database: minecraft

# 是否开启发送系统消息时的音效提示
sound-notify: true

update:
  # 是否开启自动检查更新
  check: true
  # 是否在 op 进入服务器时提醒更新
  notify: true

settings:
  nbt:
    item_pickup: "@yuillustration-item-pickup"